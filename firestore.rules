rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Cho phép đọc và ghi toàn quyền cho ứng dụng Trendify & Web Admin
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
